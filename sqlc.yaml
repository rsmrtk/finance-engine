version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/queries"
    schema: "migrations"
    gen:
      go:
        package: "dbq"
        out: "pkg/dbq"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
